{
  "scripts": {
    "dev": "yarn --cwd app vite --port 3000",
    "build": "yarn --cwd app vite build",
    "preview": "yarn --cwd app vite preview",
    "lint": "yarn --cwd app eslint"
  },
  "private": true,
  "workspaces": [
    "./",
    "./app"
  ],
  "dependencies": {
    "@project-serum/anchor": "^0.25.0",
    "@solana/spl-token": "^0.3.4"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "prettier": "^2.6.2",
    "ts-mocha": "^10.0.0",
    "typescript": "^4.3.5"
  }
}
